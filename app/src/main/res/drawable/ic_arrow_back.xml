<!--
  The toolbar back arrow. Drawn here rather than taken from
  Icons.AutoMirrored.Filled.ArrowBack, which is not on the classpath at all —
  material3 1.4.0 no longer pulls material-icons-core, and adding that
  deprecated artifact for a single arrow is a poor trade.

  Traced to match ic_settings: 24 viewBox, 1.5 stroke, round caps and joins, no
  fill, so the two icons in this app read as one set.

  autoMirrored replaces what the AutoMirrored icon would have given us: the
  manifest sets supportsRtl, and a back arrow must flip in a right-to-left
  layout.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="18dp"
    android:height="18dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:autoMirrored="true">
    <path
        android:pathData="M19,12 L5,12"
        android:strokeWidth="1.5"
        android:strokeColor="#000000"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
    <path
        android:pathData="M12,19 L5,12 L12,5"
        android:strokeWidth="1.5"
        android:strokeColor="#000000"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
